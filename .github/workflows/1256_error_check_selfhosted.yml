name: Python installation - self hosted - windows runner pro


on:
  # push:
  #   branches:
  #     - main
  workflow_dispatch:

jobs:
  setup-python-test:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: 3.10.11 # Does not work.
          # python-version: 3.11.9 # Works
          # python-version: 3.12.10 # Works
          # python-version: 3.13.11 # Works
          # python-version: 3.14.2 # Works.

      - name: Print Python version
        run: python --version
 