name: Reproduce setup-python 

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  reproduce:
    runs-on: macos-latest
    name: "Checking error"
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Installing Python 3.10
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"
          cache: "pip"